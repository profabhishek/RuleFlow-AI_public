# AI Engineering Log — Role 2 (Lokesh Singh, API & Data)

Per-role log file. Record: AI tool used, prompt, accepted code, modified code, rejected code, validation performed, timestamp.

---

## Role 2 — API & Data Layer (Lokesh Singh)

| # | Timestamp | AI tool | Prompt (summary) | Accepted | Modified | Rejected | Validation performed |
|---|-----------|---------|-------------------|----------|----------|----------|----------------------|
| 1 | 2026-07-24, 15:02 IST | Claude Code (Sonnet 5) | Design + implement an AI/NL layer per the hackathon requirement that the product itself use AI (not just AI-assisted coding): provider-agnostic `LLMProvider` interface, offline `StubProvider`, `app/config.py` + `app/dependencies.py` wiring, NL request/response schemas. | Yes, as generated | — | — | `python -m py_compile` on all new/edited files. |
| 2 | 2026-07-24, 15:02 IST | Claude Code (Sonnet 5) | Implement `app/services/rule_store.py`: SQLite-backed persistence via SQLAlchemy Core, built on R1's documented `Rule.from_dict`/`rules_to_dicts` storage contract (`rule_engine/loader.py` docstring) so no rule schema is duplicated. | Yes, as generated | — | — | End-to-end smoke test in an isolated scratch git worktree checked out from R1's `feature/rule-engine` (read-only, not merged): confirmed all 5 seed rules loaded from `rules.json` into SQLite and returned via `GET /rules`. |
| 3 | 2026-07-24, 15:02 IST | Claude Code (Sonnet 5) | Implement `POST /rules/from-text` (NL rule authoring) and `POST /decide/query` (NL query -> structured request -> `evaluate()` -> NL explanation), additive to existing structured `/rules` and `/decide` endpoints. | Yes, as generated | **Yes** — first draft's NL-explanation prompt in `decisions.py` accidentally reused a trigger phrase ("Return a JSON object of request fields") from the request-extraction prompt, which would have made the offline `StubProvider` misroute explanation calls as extraction calls. Caught before commit; rewrote the explanation system prompt to a distinct phrase. | — | Re-ran the live smoke test via `httpx.ASGITransport` against the real `rule_engine.evaluate()`: `POST /rules/from-text` created a valid rule validated through R1's own `Rule.from_dict`; `POST /decide/query` returned a correct `Decision` plus a distinct, correctly-routed NL explanation. |
| 4 | 2026-07-24, 15:02 IST | Claude Code (Sonnet 5) | Wire `app/main.py` (FastAPI app, router mounting, `/health`). | Yes, as generated | — | — | Imported `app.main` against R1's real `rule_engine` package (temporary read-only worktree, no merge); `GET /health` returned `200 {"status": "ok"}`. |
| 5 | 2026-07-24, 16:44 IST | Claude Code (Sonnet 5) | Address code review of the `feature/api` commit: add a thread-safety comment on `rule_store._get_engine()`, fix `POST /rules/from-text` missing `status_code=201` (inconsistent with `POST /rules`). | Yes, as generated | — | — | Reviewed diff manually; both are one-line, low-risk changes. |
| 6 | 2026-07-24, 17:17 IST | Claude Code (Sonnet 5) | Follow-up on review: add the previously-deferred `GET/PUT/DELETE /rules/{id}` endpoints, and fix `add_rule()` silently overwriting on a colliding id (no duplicate rejection). | Yes, as generated | User redirected the initial approach — asked what `IntegrityError` was for before approving; confirmed switching `add_rule()` from `on_conflict_do_update` (upsert) to a plain `insert()` that lets SQLite's primary-key constraint raise `IntegrityError`, caught and re-raised as `RuleAlreadyExistsError` -> 409. DB-enforced, no check-then-act race. | An ad-hoc Python smoke-test script (spin up the app in a scratch dir, hit endpoints via `httpx.ASGITransport`) was proposed for validation and explicitly rejected by the user — deferred to real `TestClient` tests once those exist, not more scratch scripts. | `python -m py_compile` only; behavioral validation deferred per user's redirect above. |
| 7 | 2026-07-24, 20:26 IST | Claude Code (Sonnet 5) | Add a Google Gemini `LLMProvider` (user confirmed a Gemini key is available). Extend the provider interface with an optional Pydantic `schema` so structured output can be requested. | Yes, as generated | User redirected the initial factory design — asked "why change the code to be like this?" before approving the eager-import dict-registry version; switched to a lazy import (`from .gemini import GeminiProvider` only inside the `if name == "gemini"` branch) so `google-genai` stays an optional dependency for teammates on the offline `StubProvider` path. User also declined an offer to scaffold a blank `.env` (see `no_touching_cred_files` memory) — code that *reads* `LLM_API_KEY` was written, the credential file itself was left untouched. | `python -m py_compile` on all new/edited files; no live Gemini call made (no key held by the assistant). |
| 8 | 2026-07-24, 20:26 IST | Claude Code (Sonnet 5) | Fix a live bug the user hit: `curl POST /rules/from-text` returned AI output wrapped in ` ```json ` fences, breaking `json.loads`. Used Gemini's structured-output mode instead of prompt-based "JSON only" instructions. | Yes, as generated | — | — | Added `AIGeneratedRule`/`AIRuleCondition` Pydantic schemas (`app/schemas.py`) mirroring `rule_engine.Rule.from_dict`'s expected shape; `GeminiProvider.complete()` sets `response_mime_type=application/json` + `response_schema` when a schema is passed, so Gemini returns exact JSON with nothing to strip. Verified the (unaffected) `StubProvider` path still dispatches correctly after the prompt-text/dispatch-phrase changes via a scratch-venv run. |
| 9 | 2026-07-24, 20:27 IST | Claude Code (Sonnet 5) | Fix a second live bug the user hit: API returned operator `"=="` for a condition, frontend dropdown silently showed `"gt"` instead. Root-caused to the engine accepting operator aliases (`>`, `==`, `matches`, `date_lt`, ...) that the frontend's canonical-only `<select>` doesn't recognize. | Yes, as generated | — | — | Audited every `@operator(...)` registration in `rule_engine/evaluators.py` line by line against the new `rule_store.normalize_rule_dict()` alias map — confirmed all 13 aliases across 11 canonical operators are covered, none missing or mis-mapped. Verified `normalize_rule_dict()` output directly in a scratch venv (`"=="` -> `"eq"`, canonical values pass through unchanged). |
| 10 | 2026-07-24, 20:37 IST | Claude Code (Sonnet 5) | User flagged the frontend operator dropdown as "a large list" (23 flat options). Clarified scope first: user confirmed `frontend/` can be treated as shared R1/R2 territory (not exclusively another role's file), and asked to group operators by category. | Yes, as generated | — | — | `node --check frontend/app.js` — grouped `OPERATOR_GROUPS` (Comparison/Equality/Boolean/String/Membership/Emptiness/Date) rendered as `<optgroup>` blocks; `OPERATORS` flat list still derived from it for `UNARY_OPS` compatibility. Not yet committed at time of writing. |

**Review disposition (external reviewer, 2026-07-24 ~16:30 IST):**
- Missing `TestClient` coverage for `/decide`, `/decide/bulk`, `/rules`, `/rules/from-text`, `/decide/query` — accepted as minor, no change required now (belongs with the R3-owned `tests/test_api_nl.py` item already flagged below).
- `/decide` and `/decide/bulk` take a raw `dict[str, Any]` body instead of a Pydantic schema (loses validation/Swagger docs) — acknowledged, acceptable for now.
- Mixed DI style (LLM provider via `Depends`, rule store via module-level `_engine` global) — acknowledged, fine for a single sync worker.
- Incomplete CRUD (`PUT/DELETE/GET /rules/{id}`) — originally deferred, added in row #6 above (commit `fbcd23f`, 17:17 IST) after user follow-up request.

### Bugs the AI introduced and how they were resolved
- **NL-explanation calls misrouted to request-extraction logic** (row #3 above): the offline `StubProvider` dispatches by checking substrings in the `system` prompt; the explanation prompt's first draft accidentally contained the same trigger phrase as the request-extraction prompt. Fixed by giving each of the three AI call sites (rule authoring, request extraction, explanation) a distinct, non-overlapping system-prompt phrase. Caught during the same session via the end-to-end smoke test, before any commit.

| 11 | 2026-07-24, 20:37 IST | Claude Code (Sonnet 5) | User asked directly whether `main.py` still had no logging wired in. Confirmed it didn't — R3's `core/logging.py`, `core/errors.py`, and `routers/audit.py` had all been merged and were fully built, just never connected. Wired `configure_logging()`, `register_exception_handlers(app)`, and `app.include_router(audit.router)` into `main.py`. | Yes, as generated | — | — | Live smoke test in a scratch venv: `/health` returns structured JSON log lines now (previously silent/unformatted), `/audit` responds `200 []`, and a deliberately-invalid `POST /rules` triggers 422. Found in the process: the 422 body is FastAPI's default `{"detail": ...}` shape, not R3's uniform `{"success": false, "message": ...}` — `rules.py` catches `RuleValidationError` and re-raises as `HTTPException` directly, which Starlette always resolves via its own default handler regardless of registered handlers for other exception types. Logged as a flag below, not changed (response-shape decision, not an obvious bug). |
| 12 | 2026-07-24, ~20:50 IST (commit 856635b) | Claude Code (Sonnet 5) | Fix a third live bug the user hit: `POST /decide/query` returned AI output wrapped in ` ```json ` fences (same class as row #8, but on the request-extraction call, which had no output enforcement). | Yes, as generated | — | — | Added a `json_mode: bool` flag to `LLMProvider.complete()` for JSON whose keys aren't known ahead of time (extracted request fields), so a fixed schema doesn't fit: Gemini sets `response_mime_type=application/json` without a `response_schema`. Swept all three `llm.complete()` call sites — rule creation uses `schema=`, request extraction now uses `json_mode=True`, explanation stays plain text (its output is prose). Verified stub dispatch still works in a scratch venv. |
| 13 | 2026-07-24, 21:57 IST | Claude Code (Sonnet 5) | Add finance-domain rule **categories / gates** (v1: tag + per-category filter + worst-wins aggregation; blocking/order deferred). After a design discussion, user chose: `category` as an R1 model field, and a new `/decide/gated` endpoint. | Yes, as generated | **Crosses into R1 files with user's explicit go-ahead** ("I write it, R1 reviews"): added `category` (default `"general"`) to `rule_engine/models.py` `Rule` + `from_dict` validation, and emitted it in `rule_engine/loader.py` `rules_to_dicts`. Engine evaluation logic untouched — category is grouping metadata only. This is a proposal for R1 to review/own via PR, not a unilateral merge. | An alternative "R2 store-only column" design (zero R1 files touched) was presented and **not** chosen by the user, who preferred the field travel with the Rule everywhere. | Ran R1's full suite (`pytest tests/test_engine.py tests/test_evaluators.py` → **46 passed**) to confirm the model change is backward-compatible. Live scratch-venv test of `/decide/gated`: multi-gate worst-wins verified — a sanctions-hit request returns REJECT (KYC gate) even though the underwriting gate APPROVEs; empty request → REVIEW; category round-trips through `from_dict`/`rules_to_dicts`. R2 parts: `list_rules(category=...)` filter, `GatedDecisionResponse`/`GateResult` schemas, `category` on `AIGeneratedRule`, frontend category input + table column (shared R1/R2 file). |
| 14 | 2026-07-25, 09:57 IST | Claude Code (Opus 4.8) | Add OpenRouter as an automatic backup LLM provider because Gemini has hard daily limits. User confirmed the design: auto fallback chain (not manual switch) + provider-specific keys held simultaneously. | Yes, as generated | Framed the design fork first (auto-failover vs manual-select; provider-specific keys vs reusing the single `LLM_API_KEY`) — user chose auto-failover + provider-specific keys. `LLM_PROVIDER` is now a comma-separated chain (e.g. `gemini,openrouter`); new `FallbackProvider` tries each in order and falls through on *any* exception (429 daily-limit, outage, timeout). New `OpenRouterProvider` (OpenAI-compatible SDK, lazy-imported). Factory rewritten to build each named provider, skip unbuildable ones (missing key/SDK, logged), and wrap multiples in the fallback. `gemini.py` now reads `GEMINI_API_KEY` with `LLM_API_KEY` as a back-compat alias. Config gains `gemini_api_key`, `openrouter_api_key`, `openrouter_model`. | — | Scratch-venv verified: `gemini,openrouter` with no keys → both skipped → `StubProvider`; `stub,stub` → `FallbackProvider`; a provider raising "429 daily limit" falls through to the next and returns its output. `OpenRouterProvider` constructs against `https://openrouter.ai/api/v1` with the default model. **No live OpenRouter/Gemini network call made — no API key held by the assistant** (see `no_touching_cred_files`); the live-key path needs the user to set `OPENROUTER_API_KEY` in their own `.env`. |
| 15 | 2026-07-25, 10:15 IST | Claude Code (Opus 4.8) | Resolve a stash-pop conflict after the user pulled: a teammate (R1/Abhishek) had independently added a `ResilientProvider` (prompt cache + degrade-to-stub on quota exhaustion, exposing `degraded`/`primary_name` to `/health` and `ai_degraded` response fields) while my stash added `FallbackProvider` (gemini→openrouter chain). Both target quota/failover but are different abstractions. | Yes — kept **both**. The conflict was only in `app/services/llm/__init__.py` (imports + `__all__`); the `get_provider` body auto-merged to my chain factory. Confirmed the two layers compose correctly: `dependencies.py` already does `ResilientProvider(get_provider(...))`, so with the chain factory it becomes `ResilientProvider(FallbackProvider([Gemini, OpenRouter]))` — Gemini 429 → FallbackProvider silently serves via OpenRouter (real AI, `degraded` stays False); only if the whole chain fails with a quota error does ResilientProvider degrade to the stub. OpenRouter now covers Gemini's daily limit *before* the offline stub, which is strictly better than the previous degrade-straight-to-stub. `config.py` auto-merged cleanly (my provider keys + teammate's `nl_explanations`). | — | Simulated the composition in a scratch venv: Gemini-429 → OpenRouter serves, not degraded; both real providers 429 → chain re-raises quota → ResilientProvider degrades to stub (`degraded=True`). Full suite green: **484 passed**; `app.main` imports clean. |

### Flagged for team consult (found while working, not resolved here — outside R2 scope)
- **`/health` `llm_active` under a chain:** R1's `ResilientProvider.primary_name` reports `type(self._primary).__name__`, which is now `"FallbackProvider"` (the chain) rather than the concrete front model like `"GeminiProvider"`. Cosmetic — health still correctly signals a real-vs-stub provider — but if R1 wants the concrete model name shown, `ResilientProvider.primary_name` could delegate through a wrapper. Left untouched (R1-owned `resilient.py`).
- **R1 model change pending review (row #13):** `category` was added to `rule_engine/models.py` + `loader.py` as a proposal with the user's go-ahead, but R1 owns those files — needs their review/sign-off before the branch merges. Engine logic is untouched; the change is additive with a default, and all 46 R1 tests pass.
- **Per-gate decision policy (deferred from v1):** all gates currently share the global `DecisionPolicy`, so a gate holding only reject-rules defaults to REVIEW for anyone who doesn't trigger it — it can never cleanly APPROVE. A pure screen like KYC would more naturally default to APPROVE (pass unless a reject fires). Real per-category policy (each `RuleSet` carrying its own mode/default) is the planned v2 and needs an R1 conversation about whether `RuleSet` lives in the engine or the R2 service layer.
- `evaluate` naming: CONTRIBUTING.md's Rule Engine Guidelines were edited to rename the per-evaluator interface to `apply()`, but R1's merged code kept `evaluate(rule, request) -> RuleResult`. Docs now describe an interface that doesn't match the merged code.
- `develop` branch: `setup.md` (from `feature/rule-engine`) tells contributors to branch off `develop`, contradicting CONTRIBUTING.md's `feature/* -> PR -> main`.
- `tests/test_api_nl.py` for the new NL endpoints should be owned/drafted by R3 per the `tests/test_api_*.py` ownership line — still open, now also covers `/rules/{id}` CRUD and the Gemini NL endpoints.
- `docs/API.md` / `PS4_Team_Project_Structure.md` should document the new endpoints — R4-owned, still open.
- Error response shape inconsistency (row #11 above): `rules.py`'s manual `HTTPException` raises bypass R3's uniform `{"success": false, "message": ...}` error format. Worth a team decision on whether routers should let engine exceptions propagate to R3's handlers instead of catching them locally, or whether R3's handler should be taught to also format `HTTPException` — affects the frontend's error-parsing contract either way.

~~`main.py` doesn't configure logging~~ — resolved, see row #11.
~~Incomplete CRUD (`PUT/DELETE/GET /rules/{id}`)~~ — resolved, see row #6.
